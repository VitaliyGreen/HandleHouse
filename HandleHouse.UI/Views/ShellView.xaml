<Window x:Class="HandleHouse.UI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HandleHouse.UI.Views"
        mc:Ignorable="d"
        Title="Handle House" Height="450" Width="800"
        FontSize="16" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="View">
                <MenuItem Header="Settlements" x:Name="ActivateSettlementsView" />
                <MenuItem Header="Houses" x:Name="ActivateHousesView" />
                <MenuItem Header="Furniture" x:Name="ActivateFurnitureView"/>
                <MenuItem Header="Works" x:Name="ActivateWorksView" />
                <MenuItem Header="Owners" x:Name="ActivateOwnersView"/>
                <MenuItem Header="People" x:Name="ActivatePeopleView"/>
            </MenuItem>
        </Menu>
        <ContentControl Grid.Row="1" x:Name="ActiveItem"/>
    </Grid>
    
</Window>
